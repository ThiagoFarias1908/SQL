INNER JOIN - É utilizada para combinar linhas de duas ou mais tabelas com base em uma condição especificada.

LEFT JOIN -  É usada para combinar registros de duas tabelas com base em uma condição especificada, onde todos os registros da tabela à esquerda são incluídos, mesmo que não haja correspondência na tabela à direita. 

RIGHT JOIN - É usada para combinar registros de duas tabelas com base em uma condição especificada, onde todos os registros da tabela à direita são incluídos, mesmo que não haja correspondência na tabela à esquerda.

CROSS JOIN - Serve para combinar todas as linhas de duas tabelas, criando um produto cartesiano entre elas.

FULL JOIN - É usada para combinar resultados de duas tabelas, retornando todas as linhas de ambas as tabelas, onde a condição de junção é atendida.
