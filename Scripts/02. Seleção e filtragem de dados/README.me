AS - É utilizada para atribuir um alias temporário a uma tabela ou a uma coluna em uma consulta.

ORDER BY - É usada para ordenar os resultados de uma consulta em ordem crescente ou decrescente com base em uma ou mais colunas especificadas.

TOP - É usada para limitar o número de linhas retornadas em uma consulta.

WITH TIES - É utilizada com comandos de ordenação para incluir linhas que têm o mesmo valor da coluna usada na ordenação final.

WHERE - É fundamental para filtrar registros de uma tabela que atendam a determinadas condições específicas.

AND - Atua como um operador lógico que só retorna linhas que atendem a todas as condições especificadas.

OR - É essencial para criar consultas que buscam registros que atendem a pelo menos uma das condições especificadas.

IN - É utilizada para especificar múltiplos valores em uma cláusula.

IS NULL - É usada para verificar se uma coluna em um banco de dados está vazia.

BETWEEN - É usada para verificar se um valor está dentro de um intervalo especificado. 

NOT - Tem a função de negar uma condição.

LIKE - É utilizada para realizar comparações de padrões em strings.
